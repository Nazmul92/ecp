__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.build-tmp/

# Local run artifacts. Audit logs can contain evidence values and prompts —
# never commit them; see PRODUCTION.md on durable sinks and redaction.
*.jsonl
audit/
*.sqlite

# Generated by examples/04_ecp_real_agent.py against whatever local model you
# have. Machine-specific latency, so it is produced on demand, not checked in.
live_run_record.json
live_run.log

# arm_comparison_record.json IS checked in: it is deterministic (MockLLM), and
# benchmark/figures.py reads it to build the two-arm figure.
!arm_comparison_record.json
